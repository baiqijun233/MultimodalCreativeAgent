# 安全说明

请不要在 Issue、Pull Request、截图或日志中公开 `DEEPSEEK_API_KEY`、`ARTCLAW_API_KEY_ACCOUNT_A` 或其他服务密钥。

如果发现密钥泄露、路径穿越、未授权付费调用或其他安全问题，请不要公开发布细节，优先通过 GitHub 私密渠道联系仓库维护者，并提供复现步骤和受影响版本。

项目默认只监听本机地址，所有可能产生费用的生成接口都要求显式确认。正式公网部署前仍需配置反向代理鉴权、HTTPS、防火墙和限流。
